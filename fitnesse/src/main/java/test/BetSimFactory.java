package test;

public class BetSimFactory {

	public static BetSim create() {
		/*
		DockerClientConfig config = DefaultDockerClientConfig.createDefaultConfigBuilder().build();
		
		DockerClient dockerClient = DockerClientBuilder.getInstance(config).build();
		
		dockerClient.pullImageCmd("wiremock/wiremock").withTag("2.32.0").start();
		
		HostConfig hostConfig = HostConfig.newHostConfig().withPortBindings(new Ports(new ExposedPort(8080), Binding.bindPort(8090))).withBinds(new Bind());
		
		CreateContainerResponse container =
				dockerClient.createContainerCmd("wiremock/wiremock:2.32.0").withName("betsim").withHostConfig(hostConfig).exec();
				*/
		return null;
	}

}
